ship_defense={["start"]=0,in_dungeon= function (idx)return idx >= 358 * 10000 and idx < ( 358 + 1 ) * 10000 
end ,main_quest_complete= function ()if 0 > 0 then 
local main_quest = pc . getf ( "main_quest_meley_trail" , "__status" ) 
if main_quest ~= nil and main_quest == main_quest_meley_trail . __COMPLETE__ then 
return true 
end 
return false 
end 
return true 
end ,check_requirements= function (remove_ticket)if ship_defense_mgr . need_party ( ) or party . is_party ( ) then 
local pids = { party . get_member_pids ( ) } 
local noQuestMembers = { } 
local notEnoughLevelMembers = { } 
local noTicketMembers = { } 
local questCheck = true 
local ticketCheck = true 
local levelCheck = true 
local ticketGroup = { get_special_item_group ( 10033 ) } 
if not party . is_party ( ) then 
syschat ( "Yolculuða yalnýzca bir grupla baþlayabilirsiniz." ) 
return false 
end 
if not party . is_leader ( ) then 
say ( "Yalnýzca grup lideri kaydolabilir." ) 
return false 
end 
for i , pid in next , pids , nil begin 
q . begin_other_pc_block ( pid ) 
if not ship_defense . main_quest_complete ( ) then 
table . insert ( noQuestMembers , pc . get_name ( ) ) 
questCheck = false 
end 
if pc . get_level ( ) < 90 then 
table . insert ( notEnoughLevelMembers , pc . get_name ( ) ) 
levelCheck = false 
end 
if ship_defense_mgr . need_ticket ( ) then 
local hasTicket = false 
for idx = 1 , table . getn ( ticketGroup ) , 2 begin 
if pc . count_item ( ticketGroup [ idx ] ) >= ticketGroup [ idx + 1 ] then 
hasTicket = true 
break 
end 
end 
if not hasTicket then 
table . insert ( noTicketMembers , pc . get_name ( ) ) 
ticketCheck = false 
end 
end 
q . end_other_pc_block ( ) 
end 
if not questCheck then 
say ( "En az bir oyuncu henüz görevi[ENTER]tamamlamadý." ) 
for i , name in next , noQuestMembers , nil begin 
say ( color ( 1 , 1 , 0 ) , " " .. name ) 
end 
if not levelCheck then 
wait ( ) 
else 
return false 
end 
end 
if not levelCheck then 
say ( "En az bir oyuncu seviye gereksinimlerini[ENTER]karþýlamýyor." ) 
for i , name in next , notEnoughLevelMembers , nil begin 
say ( color ( 1 , 1 , 0 ) , " " .. name ) 
end 
if not ticketCheck then 
wait ( ) 
else 
return false 
end 
end 
if ship_defense_mgr . need_ticket ( ) then 
if not ticketCheck then 
say ( "En az bir oyuncunun Geçiþ Bileti[ENTER] yok." ) 
for i , name in next , noTicketMembers , nil begin 
say ( color ( 1 , 1 , 0 ) , " " .. name ) 
end 
return false 
end 
end 
if party . get_near_count ( ) < table . getn ( pids ) then 
say ( "En az bir oyuncu gruptan çok uzakta." ) 
return false 
end 
if ship_defense_mgr . need_ticket ( ) and remove_ticket then 
for i , pid in next , pids , nil begin 
q . begin_other_pc_block ( pid ) 
for i = 1 , table . getn ( ticketGroup ) , 2 begin 
if pc . count_item ( ticketGroup [ i ] ) >= ticketGroup [ i + 1 ] then 
pc . remove_item ( ticketGroup [ i ] , ticketGroup [ i + 1 ] ) 
break 
end 
end 
q . end_other_pc_block ( ) 
end 
end 
else 
if ship_defense_mgr . need_ticket ( ) then 
local hasTicket = false 
for idx = 1 , table . getn ( ticketGroup ) , 2 begin 
if pc . count_item ( ticketGroup [ idx ] ) >= ticketGroup [ idx + 1 ] then 
hasTicket = true 
break 
end 
end 
if not hasTicket then 
table . insert ( noTicketMembers , pc . get_name ( ) ) 
ticketCheck = false 
end 
if not ticketCheck then 
say ( "En az bir oyuncunun Geçiþ Bileti[ENTER] yok." ) 
for i , name in next , noTicketMembers , nil begin 
say ( color ( 1 , 1 , 0 ) , " " .. name ) 
end 
return false 
end 
if remove_ticket then 
for i = 1 , table . getn ( ticketGroup ) , 2 begin 
if pc . count_item ( ticketGroup [ i ] ) >= ticketGroup [ i + 1 ] then 
pc . remove_item ( ticketGroup [ i ] , ticketGroup [ i + 1 ] ) 
break 
end 
end 
end 
end 
end 
return true 
end }